# デイリースクラム記録 - Sprint 013

## DAY1（2026-07-22 水曜日）

### 昨日やったこと

DAY1 は初日のため、なし。

### 今日やること

| メンバー          | 今日やること                                                                  |
| ----------------- | ----------------------------------------------------------------------------- |
| 伊藤（Dev）       | TASK-101 / TASK-202 / TASK-302 を進め、DAY1 の文書化と設計起点を作る          |
| 田中（Dev）       | TASK-102 / TASK-201 を反映し、`pr_checklist.md` の文体を既存運用に合わせる    |
| 山本（助っ人Dev） | TASK-103 / TASK-401 を進め、chapter01 / 02 の定義源を安全な定数として整理する |
| 中村（助っ人Dev） | TASK-203 の観点で表現曖昧さを除去し、DAY2 以降のテスト観点へつなぐ            |
| 高橋（SM）        | 障害物有無と DAY2 以降の順序を確認し、主軸 4PBI の流れを保つ                  |

### 障害物

なし

---

### 本日のインクリメント作成結果

#### PBI-058 / PBI-059: `pr_checklist.md` 更新【完了】

- `project/docs/pr_checklist.md` の **§2 テスト** に、`import.meta.env` 依存実装は直接動的書換ではなく **props 注入 / env 参照境界の分離** で検証する規律を追加
- `project/docs/pr_checklist.md` の **§4 動作確認** に、GitHub Pages など **サブパス公開時のリンク / アセット / ハッシュルーティング参照が `base` 配下で破綻しないこと** を確認する規律を追加
- どちらも既存の 1 項目 1 観点の文体に合わせ、PR レビューでそのまま確認可能な表現へ統一
- 改定履歴に Sprint013 DAY1（v0.7.0）を追記し、Sprint012 レトロ A-71 / A-72 の反映元を残した

#### PBI-056: `referenceData.ts` 定義源作成【完了】

- `project/front/src/data/referenceData.ts` を新規作成
- `ReferenceChapter` / `ReferenceSection` / `ReferenceBlock` の型を定義し、`patternData.ts` と同様に **唯一の定義源 + 配列定数 + 検索関数** の構成に整理
- chapter01 / chapter02 の内容を、段落・箇条書き・表・補足メモの安全な TypeScript 定数として構造化
- Markdown / HTML の生描画は行わず、`dangerouslySetInnerHTML` 不使用前提のレンダリングに耐える設計にした

#### PBI-060: CSS 分割試行対象の設計固定【完了】

- Sprint013 の CSS 分割試行対象を **PBI-056 の解説リファレンス画面** に固定
- 新規画面で影響範囲を局所化しやすく、既存学習導線への副作用を抑えて試せる方針で合意
- DAY2 ではこの前提で `styles.css` 分割方針比較と UI 実装着手へ進む

### DoD観点の途中確認

| 基準                                     | 状態 | 備考                                                        |
| ---------------------------------------- | ---- | ----------------------------------------------------------- |
| 文書更新が簡潔・検証可能                 | ✅   | `pr_checklist.md` で 1 項目 1 観点に統一                    |
| `dangerouslySetInnerHTML` 不使用（10-2） | ✅   | `referenceData.ts` は文字列定数のみで HTML を保持しない     |
| 唯一定義源の整合                         | ✅   | `patternData.ts` と同様に配列定義 + 検索関数の構成を採用    |
| DAY2 以降へつながる設計起点              | ✅   | 解説画面を CSS 分割試行対象に固定し、章データ基盤を先行整備 |

### スプリントゴールへの進捗

DAY1 で PBI-058 / PBI-059 を完了し、Sprint012 レトロの未着手アクション A-71 / A-72 をクローズできる状態になった。加えて PBI-056 の定義源と PBI-060 の試行対象画面を先に固めたことで、DAY2 以降は「解説」導線と CSS 分割試行を実装中心で進められる。

## DAY2（2026-07-23 木曜日）

### 昨日やったこと

| メンバー          | 昨日やったこと                                                                               |
| ----------------- | -------------------------------------------------------------------------------------------- |
| 伊藤（Dev）       | `pr_checklist.md` 追記と CSS 分割試行対象の設計固定を進め、DAY2 の導線実装に入れる土台を作成 |
| 田中（Dev）       | PR チェック観点の文体調整と CSS 分割方針比較の前半整理を行い、判断材料を揃えた               |
| 山本（助っ人Dev） | chapter01 / chapter02 の定義源を TypeScript 定数として構造化し、画面実装に渡せる状態にした   |
| 中村（助っ人Dev） | サブパス公開観点の曖昧さを除去し、DAY2 以降の UI / テスト観点に引き継いだ                    |
| 高橋（SM）        | 障害物なしを確認し、DAY2 は PBI-056 / PBI-060 の主軸実装に集中する進め方で整列した           |

### 今日やること

| メンバー          | 今日やること                                                                                     |
| ----------------- | ------------------------------------------------------------------------------------------------ |
| 伊藤（Dev）       | `TASK-402` としてホームから「解説リファレンス」へ遷移できる導線を実装する                        |
| 田中（Dev）       | `TASK-301` を確定しつつ、`TASK-403` / `TASK-303` として解説画面本体と画面単位 CSS 分割を実装する |
| 山本（助っ人Dev） | 375px 幅・両テーマ・キーボード操作の確認観点を整理し、DAY3 手動確認へ引き継ぐ                    |
| 中村（助っ人Dev） | `TASK-404` / `TASK-305` として vitest・型チェック・lint・build の検証線を整える                  |
| 高橋（SM）        | PBI-056 / PBI-060 がスプリントゴールに直結していることを再確認し、主軸以外へ流れないよう調整する |

### 障害物

なし

---

### 本日のインクリメント作成結果

#### PBI-056: 解説リファレンス画面と導線追加【主要実装完了】

- `project/front/src/pages/ReferencePage.tsx` を追加し、chapter01 / chapter02 を **1 画面・安全な JSX レンダリング** で参照できる解説リファレンス画面を実装
- `#/reference` と `#/reference/chapter01|chapter02` を `Router.tsx` に追加し、章スキップ導線とホーム復帰導線を用意
- `App.tsx` に **「解説リファレンス」 / 「パターン別解説」** のサポート導線を追加し、既存学習導線と並列アクセスできるようにした
- 表・箇条書き・補足メモは `dangerouslySetInnerHTML` を使わず、TypeScript 定義源から通常の React レンダリングで構築した

#### PBI-060: CSS 分割試行導入【試行完了】

- CSS 分割方針は **ページ単位で新規画面のみ切り出す** を採用し、`project/front/src/pages/ReferencePage.css` を新設
- 画面固有のレイアウト・カード・表・章ナビは `ReferencePage.css` に閉じ込め、既存 `styles.css` への追加はホーム導線ピルの最小差分に限定
- 375px 幅で 2 列固定にならないよう、章ナビ・メタ情報は `max-width: 480px` で 1 列に折り返すレスポンシブ構成にした

#### 検証

- `project/front/src/pages/__tests__/ReferencePage.test.tsx` を追加し、主要コンテンツ・章リンク・`dangerouslySetInnerHTML` 不使用を検証
- `project/front/src/Router.reference.test.tsx` を追加し、ホーム画面の導線表示と `#/reference` / `#/reference/chapter02` のルーティングを検証
- `pnpm test`：36 ファイル / 362 テストすべて成功
- `pnpm tsc --noEmit`：成功
- `pnpm lint`：成功
- `pnpm build`：成功

### DoD観点の途中確認

| 基準                                          | 状態 | 備考                                                       |
| --------------------------------------------- | ---- | ---------------------------------------------------------- |
| `dangerouslySetInnerHTML` 不使用（10-2）      | ✅   | 解説画面は JSX のみで表・リスト・ノートを構築              |
| 既存導線との並列アクセス                      | ✅   | ホーム画面に「解説リファレンス」導線を追加                 |
| CSS 分割の試行導入                            | ✅   | `ReferencePage.css` に画面固有スタイルを分離               |
| 自動検証の回帰なし                            | ✅   | test / tsc / lint / build 通過                             |
| 375px・両テーマ・キーボード操作の最終手動確認 | △    | レスポンシブ CSS は投入済み。DAY3 に実画面の手動確認を継続 |

### スプリントゴールへの進捗

DAY2 で「解説」導線・chapter01/02 の参照 UI・CSS 分割の初手をまとめて入れられたため、スプリントゴールのうち **「解説リファレンス画面と CSS 分割の初手を整える」** はかなり前進した。残る主な作業は 375px / 両テーマ / キーボード操作の手動確認で、DAY3 は仕上げと微修正に集中できる状態である。

## DAY3（2026-07-24 金曜日）

### 昨日やったこと

| メンバー          | 昨日やったこと                                                                       |
| ----------------- | ------------------------------------------------------------------------------------ |
| 伊藤（Dev）       | `TASK-402` を完了し、ホームから解説リファレンスへ遷移できる導線を安定化した          |
| 田中（Dev）       | `TASK-403` / `TASK-303` を進め、解説画面本体と CSS 分割試行を実装した                |
| 山本（助っ人Dev） | `TASK-401` 後半と `TASK-404` 補助レビューを行い、DAY3 手動確認に必要な観点を整理した |
| 中村（助っ人Dev） | `TASK-404` 前半を実施し、解説画面のテスト観点（導線・aria・XSS安全性）を固めた       |
| 高橋（SM）        | 主軸PBI優先を再確認し、DAY3 は手動確認と回帰検証に集中する計画へ整列した             |

### 今日やること

| メンバー          | 今日やること                                                                                               |
| ----------------- | ---------------------------------------------------------------------------------------------------------- |
| 伊藤（Dev）       | 手動確認結果のレビュー観点整理を支援し、必要な文言調整を実施する                                           |
| 田中（Dev）       | UIの副作用確認（既存画面への影響なし）をチェックし、微修正があれば最小差分で対応する                       |
| 山本（助っ人Dev） | `TASK-304` / `TASK-405` を完了し、375px・両テーマ・キーボード操作（Tab/章スキップ/フォーカス可視性）を確認 |
| 中村（助っ人Dev） | `pnpm test` / `pnpm tsc --noEmit` / `pnpm lint` / `pnpm build` を再実行し、回帰なしを確認する              |
| 高橋（SM）        | Day4 のストレッチ投入条件（主軸Done近傍・4h以上余力）を確認し、先行着手しないようコントロールする          |

### 障害物

なし

---

### 本日のインクリメント作成結果

#### TASK-304 / TASK-405: 手動確認完了【完了】

- `ReferencePage` を 375px 幅で確認し、章ナビ・メタ情報は 1 列表示へ崩れ、ページ全体で横スクロールが発生しないことを確認
- ライト / ダーク両テーマで配色破綻がないこと、カード・表・ナビの視認性が維持されることを確認
- キーボード操作（Tab移動）で「ホームに戻る」→章スキップリンク群へ順に到達できることを確認
- 章スキップ遷移（`#/reference/chapter01|chapter02`）後の見出しフォーカス可視性を強化するため、`ReferencePage.css` に `reference-page__chapter-title:focus` のアウトラインを追加

#### 回帰検証

- `pnpm test`：36 files / 362 tests すべて成功
- `pnpm tsc --noEmit`：成功
- `pnpm lint`：成功
- `pnpm build`：成功

### DoD観点の確認

| 基準                                     | 状態 | 備考                                              |
| ---------------------------------------- | ---- | ------------------------------------------------- |
| 375px・ライト/ダークで破綻なし           | ✅   | `TASK-304` 完了                                   |
| キーボード操作とフォーカス可視性         | ✅   | `TASK-405` 完了、見出しフォーカス視認性を最小修正 |
| `dangerouslySetInnerHTML` 不使用（10-2） | ✅   | 既存実装方針を維持                                |
| 回帰検証（test/tsc/lint/build）          | ✅   | すべて再実行して通過                              |

### スプリントゴールへの進捗

DAY3 で `TASK-304` / `TASK-405` を完了し、PBI-056 / PBI-060 の完了判定が可能になった。主軸4PBI（058/059/060/056）は Done 到達。DAY4 は主軸の安定維持を優先し、**余力が4h以上確保できる場合のみ** PBI-057 着手可とする。

## DAY4（2026-07-25 土曜日）

### 昨日やったこと

| メンバー          | 昨日やったこと                                                              |
| ----------------- | --------------------------------------------------------------------------- |
| 伊藤（Dev）       | 主軸PBIの統合観点レビューを実施し、導線追加時の副作用チェック観点を整理した |
| 田中（Dev）       | UI副作用確認と微修正候補の洗い出しを実施し、最小差分で収める方針を確定した  |
| 山本（助っ人Dev） | 375px・両テーマ・キーボード確認を完了し、主軸Done判定の根拠を整理した       |
| 中村（助っ人Dev） | 回帰検証（test/tsc/lint/build）を完了し、主軸4PBIのDone到達を確認した       |
| 高橋（SM）        | 余力判定（4h以上）を確認し、ストレッチ投入条件を満たすことを明確化した      |

### 今日やること

| メンバー          | 今日やること                                                                               |
| ----------------- | ------------------------------------------------------------------------------------------ |
| 伊藤（Dev）       | chapter08 参照導線（パターン一覧・詳細）の文言を簡潔に整え、既存導線との一貫性を確保する   |
| 田中（Dev）       | `ReferencePage.tsx` に chapter05 / chapter08 表示ブロックと目次導線拡張を実装する          |
| 山本（助っ人Dev） | `referenceData.ts` に chapter05 / chapter08 要点（優先順位マトリクス・代表分類）を追加する |
| 中村（助っ人Dev） | テスト更新、`Router.tsx` の章アンカー拡張、回帰検証（test/tsc/lint/build）を実施する       |
| 高橋（SM）        | ストレッチ実施中も主軸品質を崩さないよう進捗と障害物を監視し、必要時はスコープを最小化する |

### 障害物

なし

---

### 本日のインクリメント作成結果

#### PBI-057: 解説コンテンツ拡充（chapter05 / chapter08）【完了】

- `project/front/src/data/referenceData.ts` を拡張し、`chapter05`（優先順位づけ）と `chapter08`（案件パターン別攻略）を追加
- chapter05 には **緊急度×重要度マトリクス**（4象限）と **30秒優先度チェック** を実装
- chapter08 には **代表パターン分類（対外対応 / 人事 / 業務 / リスク）** と **参照導線**（`#/patterns` / `#/patterns/:id`）を実装
- `project/front/src/pages/ReferencePage.tsx` の章レンダリングを拡張し、目次リンクを chapter05 / chapter08 まで追加
- `project/front/src/Router.tsx` で `#/reference/chapter05` / `#/reference/chapter08` を解釈できるように更新
- `project/front/src/pages/ReferencePage.css` はリンク表示と章ナビの最小追記のみで対応（既存分離方針を維持）
- `ReferencePage.test.tsx` / `Router.reference.test.tsx` を更新し、拡張導線と表示を回帰検証

#### 検証

- `pnpm test`：36 files / 362 tests すべて成功
- `pnpm tsc --noEmit`：成功
- `pnpm lint`：成功
- `pnpm build`：成功

### DoD観点の確認

| 基準                                     | 状態 | 備考                                          |
| ---------------------------------------- | ---- | --------------------------------------------- |
| 章導線（目次・アンカー）拡張             | ✅   | chapter05 / chapter08 へ拡張                  |
| 優先順位マトリクスの可視化（色依存なし） | ✅   | テーブルで4象限を表示し、文言で区分を明示     |
| パターン参照導線                         | ✅   | `#/patterns` と `#/patterns/:id` の導線を追加 |
| `dangerouslySetInnerHTML` 不使用（10-2） | ✅   | 既存方針を維持                                |
| 回帰検証（test/tsc/lint/build）          | ✅   | 全コマンド通過                                |

### スプリントゴールへの進捗

DAY4 でストレッチ `PBI-057` を完了し、解説リファレンスが **基礎理解（chapter01/02）+ 実践判断（chapter05/08）** を往復できる学習ハブになった。主軸品質を維持したまま拡張できており、スプリントゴールの「今後の画面拡張を安全に進められる状態」をさらに強化できた。

## DAY5（2026-07-26 日曜日）

### 昨日やったこと

| メンバー          | 昨日やったこと                                                                              |
| ----------------- | ------------------------------------------------------------------------------------------- |
| 伊藤（Dev）       | chapter08 参照導線の文言を整え、既存導線との整合を最終確認した                              |
| 田中（Dev）       | chapter05 / chapter08 の表示・目次導線拡張を反映し、解説画面の最終差分を収束させた          |
| 山本（助っ人Dev） | `referenceData.ts` へ chapter05 / chapter08 要点を追加し、定義源の一貫性を担保した          |
| 中村（助っ人Dev） | テスト更新と回帰検証（test / tsc / lint / build）を完了し、PBI-057 Done 判定を固めた        |
| 高橋（SM）        | ストレッチ実施中も主軸品質を維持する進行を確認し、DAY5 はクローズ作業へ集中するよう調整した |

### 今日やること

| メンバー          | 今日やること                                                                 |
| ----------------- | ---------------------------------------------------------------------------- |
| 伊藤（Dev）       | 残タスク最終点検、Sprint成果物（backlog/velocity/handoff）の更新を実施する   |
| 田中（Dev）       | DoD観点の最終確認と記録文面の簡潔化を行う                                    |
| 山本（助っ人Dev） | handoff の受け手視点レビュー（読み手が次アクションを迷わないか）を実施する   |
| 中村（助っ人Dev） | 最終品質ゲート（test / tsc / lint / build）を再実行し、回帰なしを確定する    |
| 高橋（SM）        | 障害物有無を確認し、Sprint013 クローズに必要な更新漏れがないかを最終確認する |

### 障害物

なし

---

### 本日のインクリメント作成結果

#### Sprint013 クローズ作業【完了】

- 残タスク最終点検：TASK-101〜504 の未完了なし（未完了 0 件）
- 最終品質ゲート再実行：
  - `pnpm test`：36 files / 362 tests すべて成功
  - `pnpm tsc --noEmit`：成功
  - `pnpm lint`：成功
  - `pnpm build`：成功
- `handoff_for_helpers.md` を新規作成し、助っ人向け引継ぎ事項（完了範囲・確認済み観点・次スプリント持越しなし）を明文化
- `product_backlog.csv` / `product_backlog_done.csv` を更新し、Sprint013 完了 PBI（056/057/058/059/060）を Done 側へ移送
- `velocity.csv` へ Sprint013 実績（計画 8pt / 実績 10pt / 持越 0pt）を追記

#### deploy.yml の確認メモ（pushなし）

- `.github/workflows/deploy.yml` は想定通り、**PR は verify のみ / main push 時のみ build→deploy** の構成
- 既定権限 `contents: read`、`pages:write` / `id-token:write` は deploy job 限定で最小権限を維持
- DAY5 は確認のみ実施し、push / deploy の実行は行っていない

### DoD観点の最終確認

| 基準                             | 状態 | 備考                                            |
| -------------------------------- | ---- | ----------------------------------------------- |
| コード品質（型・lint・レビュー） | ✅   | tsc/lint 通過、相互レビュー完了                 |
| テスト/手動確認                  | ✅   | test 362件成功、375px/両テーマ/キーボード確認済 |
| セキュリティ/運用                | ✅   | deploy権限設計・Secrets方針を再確認             |
| ドキュメント/バックログ整合      | ✅   | daily/sprint backlog/handoff/CSV更新完了        |

### スプリントゴールへの進捗

DAY5 で品質ゲート・記録・バックログ整合まで完了し、Sprint013 は **計画8ptに対して実績10pt（ストレッチ含む）** でクローズ可能な状態になった。主軸である「運用知見固定化」「解説リファレンス整備」「CSS分割の初手」を満たし、次スプリントへ持ち越しなしで引き継げる。
