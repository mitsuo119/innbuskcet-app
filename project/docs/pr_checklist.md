# プルリクエスト チェックリスト

> Sprint004 / TASK-011（A-11）成果物。InBusket フロントエンド開発における PR 作成時の標準チェックリスト。
> Sprint レトロスペクティブで運用化を判断する試行版。

## 対象範囲

`project/front/` 配下の変更を伴うプルリクエスト全般。

---

## 0. 前提

- [ ] 作業ブランチを最新の `main` から派生・リベース済
- [ ] PR タイトル / 本文に対象 PBI ID（`PBI-XXX`）または TASK ID（`TASK-XXX`）を含めた
- [ ] Issue に `sprint/*` または `test/*` ラベルが付いている場合、PR にも同じラベルを付与した（`.github/copilot-instructions.md`）
- [ ] PR 本文に `Fixes #<issue番号>` を記載した（該当する場合）

## 1. コード品質（DoD §1）

- [ ] `pnpm lint` がエラー・警告ゼロで完了する
- [ ] `pnpm build`（= `tsc -b && vite build`）が型エラーゼロで完了する
- [ ] 新規 CSS セクション追加や文字コード影響のある編集では、保存形式が **UTF-8（BOM なし）** であることを確認した
- [ ] チーム内ペア確認（伊藤 / 田中 / 山本 / 中村のいずれか）でレビュー済

## 2. テスト（DoD §2）

- [ ] `pnpm test` が全件 PASS する（既存テストの破壊を含まない）
- [ ] 追加・変更したロジックに対応する単体テストを追加した
- [ ] テスト実装で `node:fs` 等の Node API に安易に依存せず、fixture の `import` または `vi.mock` を優先した
- [ ] `import.meta.env` 依存実装はテストで直接動的書換に頼らず、props 注入または env 参照境界の分離で検証した
- [ ] 受入基準に基づく手動動作確認をローカル（最新 Chrome）で完了した

## 3. ドキュメント（DoD §3）

- [ ] README / docs に必要な変更を反映した（起動手順・データ追加手順に影響する変更時）
- [ ] 案件データ（`src/data/cases.json`）のスキーマ変更時はドキュメントも更新した

## 4. 動作確認（DoD §4 / §6）

- [ ] 出題 → 回答 → 解説 → 次問のサイクルが破綻なく動作する
- [ ] 出題切替の応答が体感 1 秒以内である
- [ ] GitHub Pages などサブパス公開を伴う変更では、リンク / アセット / ハッシュルーティング参照が `vite.config.ts` の `base` 配下で破綻しないことを確認した

## 5. セキュリティ（DoD §5）

- [ ] シークレット情報（API キー、トークン、パスワード等）をコードにハードコードしていない
- [ ] 新規 npm 依存を追加した場合、`pnpm audit` で High / Critical 脆弱性がないことを確認した
- [ ] メジャーバージョン更新を行う場合、PO（鈴木）の事前合意を得た（A-7 ルール）

## 6. UI / UX（DoD §7）

- [ ] スマホ幅（〜480px）〜 PC 幅で表示崩れがないことを確認した
- [ ] 主要操作（回答・次問）が 1〜2 タップ／キー入力で完結する
- [ ] **モバイル幅 375px 相当（iPhone SE/13 mini 等）で横スクロールが発生しない**（A-55 / Sprint010〜）
- [ ] **主要操作ボタン・トグル・ConfirmDialog ボタンのタップ領域が 44px 以上**（誤タップ防止 / A-55）
- [ ] **モバイルソフトキーボード表示時、回答導線・主要ボタンが画面下端に被らずタップ可能**（A-55）
- [ ] **モバイル幅でもライト / ダーク両テーマで WCAG 2.1 AA（4.5:1）コントラストを維持**（A-55）
- [ ] モバイル幅変更を伴う PR では iPhone Safari / Android Chrome 現行主要版での手動確認結果を PR 説明欄に記録した（PBI-045 関連）

## 7. アクセシビリティ（DoD §9 / `project/docs/a11y_checklist.md`）

- [ ] §9-1 主要操作（A/B/C 回答・次問・もう一度・モード切替・テーマ切替）がキーボードのみで完結する
- [ ] §9-2 フォーカス状態が視覚的に判別可能（`:focus-visible` 等）
- [ ] §9-3 操作可能要素に適切な `role` / `aria-*` が付与されている
- [ ] 色のみで意味を伝えていない（記号・テキスト併記）
- [ ] ライト / ダーク両テーマで WCAG 2.1 AA コントラスト比 4.5:1 以上を満たす（テーマ関連変更時）
- [ ] 学習スタイル切替（PBI-036）／意味ラベル統一（PBI-035）／履歴・正答率の Quick/Deep 別表示（PBI-037）に該当する変更時、`a11y_checklist.md` の 3-12〜3-19 / 5-5〜5-6 を確認した

## 8. 案件データ追加（A-11 / `project/docs/case_pattern_mapping.md`）

> 案件データ（`cases.json`）追加・改廃を伴う PR のみ対象。

- [ ] 追加後、A / B / C の各優先度は **累計 30 件以上では 30%±10% を目安**に維持する（10 件単位ではストーリー自然性を優先してよいが、特定優先度を極端に減らさない）
- [ ] `case_pattern_mapping.md` の章節マッピング（ref/chapter08-case-patterns.md）を更新した
- [ ] 受入基準として「優先度判定」と「解説本文」が一次資料（ref/）に基づくことを記録した

## 9. 入力検証・データ保護（DoD §10 / Sprint005 で初適用）

> ユーザ入力（自由記述・数値・選択値）または永続化データを扱う PR のみ対象。

- [ ] §10-1: 入力に対し最大長 / 型 / 必須・任意 の妥当性検証を実装し、境界値の単体テストで検証した
- [ ] §10-2: ユーザ入力を DOM に反映する箇所で `dangerouslySetInnerHTML` / `innerHTML` を使用していない（React テキスト描画のみ）
- [ ] §10-3: セッション内 / 永続化データの読み込み時にスキーマ整合性（必須キー存在・型一致）を検証し、不整合時は安全にフォールバックする

## 9.5 ドメイン関数契約 × 呼び出し側データ型整合（A-38 / Sprint008 TASK-017）

> ドメイン純粋関数（`domain/*`）の追加・変更を伴う PR、または既存ドメイン関数の呼び出し側コンポーネントを変更する PR が対象。中村の一次情報主義レビュー観点をプラクティス化したもの。

- [ ] ドメイン関数のシグネチャ（引数・戻り値・例外/フォールバック）が JSDoc または隣接コメントで明示されている
- [ ] 呼び出し側（`ui/*` / `App.tsx`）で渡すデータの型がドメイン関数の引数型と一致する（`as` 強制キャストではなく型ガードで narrow する）
- [ ] ドメイン関数が空配列・空文字列・`undefined` を受け取った場合の安全な戻り値が単体テストで検証されている（DoD §10-1 境界値）
- [ ] 永続化データ（`localStorage` / `sessionStorage`）から復元した値はドメイン関数に渡す前にスキーマ検証する（DoD §10-3）
- [ ] 同じラベル文字列が複数ファイルに散在している場合、唯一定義源（例: `domain/priorityLabel.ts` パターン）に集約する余地がないか確認する

## 10. コミット / PR 体裁

- [ ] コミットメッセージは Conventional Commits 風（`feat(front): ...`, `fix(front): ...` 等）
- [ ] PR タイトルに変更概要を簡潔に記載
- [ ] 大きな変更（>500 行）は分割を検討した

---

## 改定履歴

| 日付       | 版     | 概要                                                                                                                                                                                                                                                               |
| ---------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2026-05-21 | v0.1.0 | Sprint004 / TASK-011 で雛形作成（A-11）。Sprint004 レトロで運用化判断予定。                                                                                                                                                                                        |
| 2026-06-02 | v0.2.0 | Sprint005 / TASK-012（A-15 初運用）。§9 入力検証・データ保護セクションを追加（DoD §10-1/§10-2/§10-3）。cases.json `modelAnswer` 拡張 PR で初適用し、A/B/C 30% 維持・スキーマ整合性検証を確認した。                                                                 |
| 2026-06-09 | v0.3.0 | Sprint006 完了（PBI-035/036/037）。§7 a11y に学習スタイル切替・意味ラベル統一・Quick/Deep 別履歴/正答率の確認項目（a11y_checklist 3-12〜3-19 / 5-5〜5-6）を追記。§9 §10-3 に `inbasket.learningStyle.v1` localStorage キー（不正値→`deep` フォールバック）を適用。 |
| 2026-06-23 | v0.4.0 | Sprint008 / TASK-017（A-38）で §9.5「ドメイン関数契約 × 呼び出し側データ型整合」セクションを追加。中村の一次情報主義レビュー観点をプラクティス化（ドメイン関数シグネチャ明示・型ガード narrow・境界値テスト・スキーマ検証・唯一定義源集約）。                      |
| 2026-05-02 | v0.5.0 | Sprint009 リファインメントで A-47 / A-48 / A-49 を反映。UTF-8（BOM なし）確認、テストでの Node API 依存回避、案件追加時の A/B/C 分布を「累計 30 件以上で 30%±10%」目安に見直した。                                                                                 |
| 2026-05-03 | v0.6.0 | Sprint010 リファインメント（A-55）。§6 にモバイル幅 375px チェック観点（横スクロール / タップ領域 44px / ソフトキーボード被り / 両テーマ AA / 実機確認記録）を追加し全 PR で運用開始。PBI-045 / PBI-047 と連動。                                                   |
| 2026-07-22 | v0.7.0 | Sprint013 DAY1（PBI-058 / PBI-059）。§2 に `import.meta.env` 依存テスト規律、§4 にサブパス公開時のベースパス確認を追加。Sprint012 レトロ A-71 / A-72 の運用知見を PR チェックへ反映。                                                                              |
