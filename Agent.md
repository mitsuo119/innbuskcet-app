# Agent.md

このリポジトリで作業するエージェント向けの運用ルール。

## 目的

- ユーザの追加指示なしで、作業完了時に自動コミットまで実施する。
- スクラム成果物とコード成果物の変更履歴を常に追跡可能にする。

## 基本方針

1. ユーザ要求に対する編集が完了したら、可能な範囲で検証（lint/test/build など）を実施する。
2. 検証が通ったら、**自動で `git add -A` → `git commit`** を行う。
3. 1つの依頼で複数種類の変更がある場合も、原則1コミットにまとめる。
4. 失敗した検証がある場合は、修正を試みたうえでコミット可否を判断し、結果を明示する。
5. ユーザが「コミットしないで」と明示した場合のみ自動コミットを停止する。

## コミットメッセージ規約

- 形式: `type(scope): 要約`
- 例:
  - `docs(scrum): order003を作成しSprint003引継ぎを追加`
  - `chore(agent): Agent.mdを追加し自動コミット方針を定義`
  - `feat(front): 正誤履歴表示を追加`

### type の使い分け

- `feat`: ユーザ機能追加
- `fix`: 不具合修正
- `docs`: ドキュメント更新
- `refactor`: 振る舞いを変えない構造改善
- `test`: テスト追加/修正
- `chore`: 依存更新・運用ファイル更新

## 実行手順（標準）

1. 変更を保存
2. 必要検証を実行
3. `git status` で差分確認
4. `git add -A`
5. `git commit -m "<type(scope): 要約>"`
6. コミット結果（hash / 変更ファイル）をユーザへ報告

## 補足

- スクラム関連ファイル（`scrum/`）とアプリ本体（`project/front/`）を同時に更新した場合、メッセージの scope は `scrum` を優先し、本文要約でコード変更も触れる。
- 機密情報・シークレットは絶対にコミットしない。
